<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".ui.scrapbookview.PhotoDialogFragment">

    <data>
        <variable
            name="viewmodel"
            type="com.group04.scrapbookwidget.ui.scrapbookview.PhotoViewModel" />
    </data>
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#00000000">

        <FrameLayout
            android:id="@+id/photo_front"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:elevation="8dp"
            app:layout_constraintWidth_percent="0.85"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent">

            <ImageView
                android:id="@+id/zoom_photo"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:adjustViewBounds="true"
                android:scaleType="fitCenter" />

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:layout_gravity="bottom|end">
                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btnReact"
                    style="?attr/materialIconButtonOutlinedStyle"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:checkable="true"
                    app:checked="@{viewmodel.isCheckedLiveData}"
                    android:onClick="@{() -> viewmodel.isCheckedChanged()}"
                    app:icon="@drawable/toggle_heart_icon_button_selector"/>
            </LinearLayout>

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Click to view caption..."
                android:textSize="10sp"
                android:textColor="#FFFFFF"
                android:gravity="center"
                android:background="#80000000"
                android:padding="4dp"
                android:layout_gravity="bottom" />
        </FrameLayout>

        <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/photo_back"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:alpha="0"
            android:elevation="8dp"
            app:layout_constraintTop_toTopOf="@id/photo_front"
            app:layout_constraintBottom_toBottomOf="@id/photo_front"
            app:layout_constraintStart_toStartOf="@id/photo_front"
            app:layout_constraintEnd_toEndOf="@id/photo_front">

            <ImageView
                android:id="@+id/back_silhouette"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:adjustViewBounds="true"
                android:scaleType="fitCenter" />

            <TextView
                android:id="@+id/hidden_text"
                android:text="Caption here..."
                android:layout_width="0dp"
                android:layout_height="0dp"
                android:textSize="18sp"
                android:textStyle="italic"
                android:textColor="#5D4037"
                android:gravity="center"
                android:textAlignment="center"
                android:fontFamily="monospace"
                android:padding="24dp"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent" />

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Click to flip..."
                android:textSize="10sp"
                android:textColor="#999999"
                android:gravity="center"
                android:layout_margin="12dp"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent" />
        </androidx.constraintlayout.widget.ConstraintLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>