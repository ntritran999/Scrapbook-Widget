<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ScrapbookWidget" parent="Theme.Material3.Dark.NoActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.ScrapbookWidget" parent="Base.Theme.ScrapbookWidget" />

    <!-- Theme dành riêng cho uCrop để tránh lỗi màn hình đen -->
    <style name="Theme.UCrop" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
    </style>

    <style name="CircleAppearance">
        <item name="cornerSize">50%</item>
    </style>

    <style name="CircleImage" parent="CircleAppearance" />

    <style name="Theme.ScrapbookWidget.AppWidgetContainerParent" parent="@android:style/Theme.DeviceDefault">
        <item name="appWidgetRadius">16dp</item>
        <item name="appWidgetInnerRadius">8dp</item>
    </style>

    <style name="Theme.ScrapbookWidget.AppWidgetContainer" parent="Theme.ScrapbookWidget.AppWidgetContainerParent">
        <item name="appWidgetPadding">16dp</item>
    </style>
</resources>
